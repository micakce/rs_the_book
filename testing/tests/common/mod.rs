use adder;


/* We might need a module to do some setup for our tests, and we dont want this
   showing in our test results, we can do this by createing a new module in it's own 
   subfolder with <moduleName>/mod.rs
*/
pub fn setup() {
    println!("Todo listo");
}
